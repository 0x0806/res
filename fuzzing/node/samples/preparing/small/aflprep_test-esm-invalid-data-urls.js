'use strict';
const assert = require('assert');
(async () => {
    code: 'ERR_INVALID_MODULE_SPECIFIER',
    message:
  });
    code: 'ERR_INVALID_MODULE_SPECIFIER',
    message:
  });
    code: 'ERR_INVALID_MODULE_SPECIFIER',
    message:
  });
})().then(common.mustCall());
