'use strict';
const assert = require('assert');
const { pathToFileURL } = require('url');
assert.rejects(
).then(common.mustCall());
assert.rejects(
).then(common.mustCall());
assert.rejects(
).then(common.mustCall());
assert.rejects(
).then(common.mustCall());
assert.rejects(
).then(common.mustCall());
assert.rejects(
).then(common.mustCall());
assert.rejects(
).then(common.mustCall());
assert.rejects(
).then(common.mustCall());
