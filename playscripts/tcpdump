list i-faces:
# tcpdump -D

usually catch:
# tcpdump -i eth1

usually catch (more info -v, -vv, -vvv):
# tcpdump -v -i eth1

usually catch in file:
# tcpdump -i eth1 -w file.pcap

select:
# tcpdump -i eth1 port 80 -n | grep some_value_filtering --line-buffered
